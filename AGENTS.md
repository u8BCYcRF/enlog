# Enlog 更新ルール

このファイルは、Codexを含む作業者がEnlogの記録を更新するときのルールを定める。

## Source of Truth

- `data/*.tsv` を事実情報のSource of Truth（正本）とする。
- `README.md`、`logs/*.md`、`articles/*.md`はTSVから参照・派生する文書であり、TSVの事実と矛盾させない。
- 記事の読みやすさを理由に、生データを改変しない。
- 本人の感想・判断・背景は `logs/activity.md` に記録し、事実データと分離する。

## 事実データの記録

- ユーザーが明示していない日付、金額、状態、理由、結果を推測で補完しない。
- 不明値を無理に埋めない。TSVでは空欄とし、必要なら `status` や `note` に「未確定」と明記する。
- 日付は `YYYY-MM-DD`、金額は日本円の整数とし、桁区切りのカンマや通貨記号を入れない。
- 過去の事実と将来の予定を区別し、`status` に「支払済」「実施済」「予定」「継続」「保留」「金額未確定」など適切な状態を書く。
- 予定日は、その日を過ぎただけでは実施済みに変更しない。ユーザーから完了情報が得られた場合のみ更新する。
- `timeline.tsv` の `days_from_activity_start` は、活動開始基準日 `2026-08-03` との差を暦日で機械的に計算する。過去は負数、当日は `0`、未来は正数とする。
- 費用の発生日をすべて `timeline.tsv` に複製しない。活動の流れを説明する主要イベントだけを記録する。

## 費用の重複防止

- `costs.tsv` の `amount_jpy` は、その行で実際に集計へ加える金額だけを記録する。未確定額や料金定義だけの行は空欄にする。
- 新しい情報を追記する前に、同じ日付・項目・契約・請求月の既存行がないか確認する。
- 金額の訂正や「予定」から「実施済」への変更では、新規の重複行を作る前に既存レコードの更新が適切か判断する。
- 一括契約済みサービスは契約行で一度だけ計上する。実施履歴は `cost_scope=progress`、追加費用なしなら `amount_jpy=0` とし、契約料金を再計上しない。
- 月会費の料金定義行と、実際の請求実績を区別する。請求実績は対象月が特定できる形で追加し、初月割引分や既存の初期費用と二重計上しない。
- 日常生活でも価値が残る支出は `self_investment`、通常の日常費は `excluded` とし、婚活の直接費と混ぜない。
- `planned` と `excluded` は婚活実費の合計に含めない。`progress` は実施履歴であり、追加費用だけを計上する。

## 更新手順

1. 関連するTSVとMarkdownを読み、既存レコードとの重複を確認する。
2. 新情報が事実、予定、感想のどれに当たるかを判定する。
3. 事実・予定は該当TSVへ、感想・背景は `logs/activity.md` へ反映する。
4. TSVを更新したら、列数、日付形式、数値、状態、重複、費用分類を検証する。
5. READMEや記事Markdownに影響する場合は、TSVと矛盾しないよう派生文書を更新する。

## 記事化とプライバシー

- 個人情報の判定、非識別化、公開前監査は `PRIVACY.md` に従う。
- ユーザーのチャットに本人または第三者の個人情報が含まれていても、その情報はファイルへ保存せず、活動の理解に必要な非識別情報だけを要約して記録する。
- 第三者とのメッセージや会話は逐語で保存せず、結果または本人の受け止めとして一般化する。
- 本人が公開を許可した年齢と生年は保存できる。地域、職業など、それ以外の活動記録に不要な本人属性は保存しない。
- 記事では「本人の実体験」「本人の感想」「一般論」を明確に分ける。
- ZWEIやIBJの制度・料金・ルールを一般論として書く場合は、体験時点の情報であることを示し、必要に応じて公開時点の公式情報を確認する。
- 相手女性など第三者の氏名、勤務先、詳細なプロフィール、連絡先その他の識別情報を保存しない。
- 相手ごとの管理が必要になった場合は匿名IDを使用し、公開用データではさらに必要な一般化を行う。
- 本人についても、公開記事に不要な特定情報を入れない。
- 未経験の出来事について、教訓や結論を先回りして作らない。

## スキーマ変更

- 現在必要のない列やファイルを先回りして増やさない。
- 新しい記録要件が既存スキーマで表せなくなった時点で、最小限の拡張を行う。
- TSVの列を変更した場合は、READMEの説明と集計方法も同時に更新する。
